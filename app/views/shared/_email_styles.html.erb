<style>
  body { 
    font-family: Arial, sans-serif; 
    line-height: 1.6; 
    color: #333; 
    max-width: 600px; 
    margin: 0 auto; 
    padding: 20px; 
  }
  .header { 
    color: white; 
    padding: 20px; 
    text-align: center; 
    border-radius: 8px; 
    margin-bottom: 20px; 
  }
  .header-green { background: #10b981; }
  .header-blue { background: #3b82f6; }
  .header-red { background: #ef4444; }
  
  .button { 
    display: inline-block; 
    color: white; 
    padding: 12px 24px; 
    text-decoration: none; 
    border-radius: 5px; 
    font-weight: bold; 
  }
  .button-green { background: #10b981; }
  .button-blue { background: #3b82f6; }
  
  .badge { 
    padding: 4px 8px; 
    border-radius: 4px; 
    font-size: 12px; 
    font-weight: bold; 
  }
  .badge-admin { background: #fbbf24; color: #92400e; }
  .badge-signup { background: #dbeafe; color: #1e40af; }
  
  .steps { 
    background: #f8fafc; 
    padding: 15px; 
    border-radius: 5px; 
    margin: 20px 0; 
  }
  .link-text { 
    color: #666; 
    font-size: 14px; 
  }
</style>